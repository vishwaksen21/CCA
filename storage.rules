rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to read uploaded images
    match /uploads/{allPaths=**} {
      allow read: if true;
      allow write: if true; // TODO: Restrict to authenticated admins only
    }
  }
}
